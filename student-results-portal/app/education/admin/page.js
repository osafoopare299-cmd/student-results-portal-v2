import Workspace from '../workspace';

export const metadata = { title: 'Administrator Portal | Dropare Education' };

export default function EducationAdminPortal(){ return <Workspace role="admin"/>; }
