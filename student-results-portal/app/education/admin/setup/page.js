'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import { ArrowLeft, CheckCircle2, Database, RefreshCw, ShieldCheck, TriangleAlert, UploadCloud } from 'lucide-react';

export default function EducationSetupPage(){
  const [data,setData]=useState(null),[storage,setStorage]=useState(null),[message,setMessage]=useState('Checking isolated education database…'),[busy,setBusy]=useState(false);
  async function load(){setBusy(true);try{const [r,sr]=await Promise.all([fetch('/api/education/admin/setup',{cache:'no-store'}),fetch('/api/education/admin/storage-setup',{cache:'no-store'})]);const [d,sd]=await Promise.all([r.json(),sr.json()]);setData(d);setStorage(sr.ok?sd:null);setMessage(r.ok?'':(d.error||'Unable to check database.'));}catch(e){setMessage(e.message||'Unable to check database.');}finally{setBusy(false);}}
  useEffect(()=>{load();},[]);
  async function initialize(){setBusy(true);setMessage('Updating education tables on the isolated database…');try{const r=await fetch('/api/education/admin/setup',{method:'POST'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Setup failed.');const sr=await fetch('/api/education/admin/storage-setup',{method:'POST'});const sd=await sr.json();if(!sr.ok)throw new Error(sd.error||'File storage schema setup failed.');setData(d);setStorage(sd);setMessage('Education database updated successfully.');}catch(e){setMessage(e.message||'Setup failed.');}finally{setBusy(false);}}
  async function initializeStorage(){setBusy(true);setMessage('Initializing learning file metadata columns…');try{const r=await fetch('/api/education/admin/storage-setup',{method:'POST'}),d=await r.json();if(!r.ok)throw new Error(d.error);setStorage(d);setMessage(d.blobConfigured?'Learning file storage is ready.':'File database columns are ready. Connect a Vercel Blob store to activate uploads.');}catch(e){setMessage(e.message||'Unable to initialize file storage.');}finally{setBusy(false);}}
  const ready=Boolean(data?.status?.foundation&&data?.status?.materials&&data?.status?.assessments);
  const card={background:'#fff',border:'1px solid #dfeae5',borderRadius:20,padding:22};
  return <main style={{minHeight:'100vh',background:'#f5faf8',padding:24,fontFamily:'Arial,sans-serif',color:'#153126'}}><section style={{maxWidth:900,margin:'auto'}}>
    <Link href="/education/admin" style={{color:'#08744d',fontWeight:800,textDecoration:'none',display:'inline-flex',alignItems:'center',gap:7}}><ArrowLeft size={18}/> Administrator dashboard</Link>
    <div style={{margin:'24px 0 18px',display:'flex',gap:16,alignItems:'center'}}><span style={{width:54,height:54,borderRadius:16,background:'#e6f7ef',display:'grid',placeItems:'center'}}><Database size={28}/></span><div><small style={{fontWeight:800,color:'#08744d'}}>DROPARE EDUCATION</small><h1 style={{fontSize:36,margin:'4px 0'}}>Database Setup</h1><p style={{margin:0,color:'#657d74'}}>Initialize or safely extend only the isolated education database. The Student Results database remains separate.</p></div></div>
    <section style={card}><div style={{display:'flex',justifyContent:'space-between',gap:12,alignItems:'center',flexWrap:'wrap'}}><div><h2 style={{margin:'0 0 6px'}}>Isolation status</h2><p style={{margin:0,color:'#657d74'}}>{data?.configured?'EDUCATION_DATABASE_URL is configured for this preview.':'EDUCATION_DATABASE_URL has not been configured for this preview yet.'}</p></div><button onClick={load} disabled={busy} style={{border:'1px solid #cfe3d9',background:'#fff',borderRadius:11,padding:'10px 13px',fontWeight:800,color:'#08744d',display:'inline-flex',gap:7,alignItems:'center'}}><RefreshCw size={17}/> Recheck</button></div>
      <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:12,marginTop:18}}><Status label="Separate education URL" ok={Boolean(data?.configured)}/><Status label="Database isolated" ok={Boolean(data?.isolated)}/><Status label="Foundation tables" ok={Boolean(data?.status?.foundation)}/><Status label="Learning materials" ok={Boolean(data?.status?.materials)}/><Status label="Assessment system" ok={Boolean(data?.status?.assessments)}/></div>
    </section>
    <section style={{...card,marginTop:16}}><div style={{display:'flex',gap:12,alignItems:'flex-start'}}><UploadCloud size={24} color="#08744d"/><div style={{flex:1}}><h2 style={{margin:'0 0 7px'}}>Learning file storage</h2><p style={{margin:'0 0 14px',color:'#657d74'}}>Private PDF and video uploads use Vercel Blob. File metadata stays in the isolated Education database; students receive files only through authenticated course-enrolment checks.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:10,marginBottom:14}}><Status label="File database columns" ok={Boolean(storage?.databaseReady)}/><Status label="Vercel Blob connected" ok={Boolean(storage?.blobConfigured)}/></div><button onClick={initializeStorage} disabled={busy||!data?.configured||!data?.isolated||storage?.databaseReady} style={{background:'#e8f7f0',color:'#08744d',border:0,borderRadius:11,padding:'11px 14px',fontWeight:800,opacity:(busy||!data?.configured||!data?.isolated||storage?.databaseReady)?.55:1}}>{storage?.databaseReady?'File columns ready':'Initialize file columns'}</button>{storage?.databaseReady&&!storage?.blobConfigured&&<p style={{margin:'12px 0 0',padding:12,borderRadius:10,background:'#fff6df',color:'#7a5a14'}}>Database support is ready. The remaining activation step is to connect a Vercel Blob store to this project; uploads will then enable automatically.</p>}</div></div></section>
    <section style={{...card,marginTop:16}}><div style={{display:'flex',gap:12,alignItems:'flex-start'}}>{ready?<CheckCircle2 size={24} color="#08744d"/>:<ShieldCheck size={24} color="#08744d"/>}<div><h2 style={{margin:'0 0 7px'}}>{ready?'Education database ready':'Safe education database update'}</h2><p style={{margin:'0 0 15px',color:'#657d74'}}>This action is idempotent and restricted to the isolated education connection. It creates missing <b>edu_*</b> tables and indexes only; it does not modify the original results tables.</p><button onClick={initialize} disabled={busy||!data?.configured||!data?.isolated||ready} style={{background:'#08744d',color:'#fff',border:0,borderRadius:12,padding:'13px 17px',fontWeight:800,opacity:(busy||!data?.configured||!data?.isolated||ready)?.55:1}}>{busy?'Working…':ready?'All modules initialized':'Update education database'}</button></div></div></section>
    {message&&<p style={{marginTop:16,padding:14,borderRadius:12,background:'#fff4df',display:'flex',alignItems:'center',gap:8}}><TriangleAlert size={18}/>{message}</p>}
  </section></main>;
}

function Status({label,ok}){return <div style={{padding:14,border:'1px solid #e2ece7',borderRadius:14,background:'#fbfdfc'}}><div style={{display:'flex',alignItems:'center',gap:8,fontWeight:800}}>{ok?<CheckCircle2 size={18} color="#08744d"/>:<TriangleAlert size={18} color="#9a5a12"/>}{label}</div><small style={{display:'block',marginTop:6,color:'#6d8179'}}>{ok?'Ready':'Not ready'}</small></div>}
